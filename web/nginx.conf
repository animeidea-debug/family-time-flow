# FamilyTimeFlow — nginx location fragment
#
# This file is deployed as conf.d/projects/family-time-flow.conf
# to an existing nginx instance that already has this in its config:
#
#   include /etc/nginx/conf.d/projects/*.conf;
#
# Do NOT use this as a standalone nginx.conf.

# See conf.d/family-time-flow.conf for the actual configuration.